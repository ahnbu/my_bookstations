---
title: "데모모드-안내띠-상단분리-구현계획(실행완료)"
created: "2026-08-18 16:29"
session_id: "6ee47e9b-e84d-4cc1-ab7e-735c1ecdbe1e"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-my-bookstation/6ee47e9b-e84d-4cc1-ab7e-735c1ecdbe1e.jsonl"
ai: "claude"
model: "independent-lite-reviewer"
performed_at: "2026-08-18 16:29:23"
---

---
title: Done Check Lite Reviewer Report (14절 후속 조정)
created: 2026-08-18 16:20
session_id: 6ee47e9b-e84d-4cc1-ab7e-735c1ecdbe1e
session_path: C:/Users/ahnbu/.claude/projects/D--vibe-coding-my-bookstation/6ee47e9b-e84d-4cc1-ab7e-735c1ecdbe1e.jsonl
ai: claude
---

# Done Check Lite Reviewer Report

검수 대상: `docs/20260818_04_데모모드-안내띠-상단분리-구현계획(실행완료).md`의 14절 후속 조정 (14-1 ~ 14-5)

## Verdict
완료

## Requirement Audit

| 요구사항 | 상태 | 근거 |
|---|---|---|
| 1. 배너 색 톤다운 (연한 파랑 + 하단 경계선) | 충족 | `index.css`에 `.demo-mode-banner{background:#eff6ff;color:#1e40af;border-bottom:1px solid #bfdbfe}` 신설, `bg-blue-600 text-white` 완전 제거 |
| 2. 문구 변경 + 재고 기준일 제거 | 충족 | `DemoModeBanner.tsx`에서 `DEMO_SNAPSHOT_DATE` import 제거, 문구가 `미리보기 - 로그인하시면 나만의 서재를 만듭니다`로 정확히 일치 |
| 3. 다크 모드 앱 테마 연동 (index.css 전용 규칙) | 충족 (실측 완료) | `DemoModeBanner.tsx`에서 Tailwind `dark:` variant 완전 제거(주석에서만 언급). `index.css`에 `.dark .demo-mode-banner` 규칙 추가. 아래 독립 실측으로 확증 |

**독립 실측 (기존 검증 결과를 그대로 신뢰하지 않고 직접 수행)**

dev 서버(`http://localhost:5173`)를 띄우고 Playwright가 아닌 별도 브라우저 세션으로 두 차례 실측:

1. OS `colorScheme: dark`, `body.className` 초기값 `dark bg-primary text-primary` → 배너 배경 `rgb(23, 37, 84)`(다크 팔레트). `body`에서 `dark`→`light`로 클래스만 바꾸면 `rgb(239, 246, 255)`(라이트 팔레트)로 즉시 바뀌고, 다시 `dark`로 돌리면 `rgb(23, 37, 84)`로 복귀.
2. OS `colorScheme: light`, 앱 기본 테마 `light`(배너 `rgb(239, 246, 255)`) 상태에서 `body` 클래스만 강제로 `dark`로 바꾸면 OS는 여전히 light인데도 배너가 `rgb(23, 37, 84)`로 전환됨 — **OS 선호가 아니라 `body`의 `.dark`/`.light` 클래스에만 반응**함을 확인. 값은 `index.css`의 hex 값과 정확히 일치(`#eff6ff`=rgb(239,246,255), `#172554`=rgb(23,37,84)).

이 결과는 V-18의 단언(`whenLight`≠`whenDark`, 각각 정확한 rgb 값과 일치)과 완전히 부합하며, `node --test tests/demo-library-ui.test.mjs`를 재실행해 pass 1/fail 0을 재확인했다.

## Blocking Gaps
없음.

## Evidence

- V-18 단언은 형식적이지 않다. `notEqual` 뿐 아니라 라이트/다크 각각의 정확한 `rgb()` 값을 단언하므로, "아무 값이나 바뀌면 통과"하는 헐거운 테스트가 아니다. 조작 흔적 없음.
- V-14는 `[data-testid="demo-library"]` 내부 텍스트만 검사하므로 배너(그 바깥에 위치)의 신규 문구 `미리보기`와 충돌하지 않는다 — 오탐 없음. `tests/demo-library-ui.test.mjs:166-173` 확인.
- V-17은 375px에서 배너가 1줄임을 높이로 단언하고 신규 문구 포함 여부를 별도로 확인 — 재고 기준일 제거와 무관하게 유효.
- `git diff --stat`: 변경 파일 5개(`DemoModeBanner.tsx`, `index.css`, `tests/demo-library-ui.test.mjs`, `docs/DEVELOPMENT.md`, 계획서) 그대로. `index.css`에 배너 규칙 외 추가 규칙 없음(diff 확인). Scope creep 없음.
- `components/SettingsModal.tsx`, `components/KeywordSearchModal.tsx`는 `dark:` 사용 3건 그대로 남아 있음(문서가 "별도 작업으로 분리했다"고 명시한 것과 일치, 손대지 않음이 맞음).
- `npx tsc --noEmit` 재실행: `temp/기존/APITest.tsx` 5건만 남고 신규/수정 파일 오류 0건 — 보고된 베이스라인과 일치.
- `docs/DEVELOPMENT.md`의 hex 값(`#eff6ff`/`#1e40af`, `#172554`/`#bfdbfe`)이 `index.css` 실제 값과 정확히 일치. `stores/useSettingsStore.ts:419` 참조도 `applyTheme` 함수 내 해당 라인과 근접해 타당.
- 계획서 14절 서술(14-1~14-5)이 실제 diff·실측과 불일치하는 과장 없음. "실측으로 확증했다"는 문구가 실제로 근거 있는 수치(body.className, 배경 oklch/rgb 값)를 동반함.
- `temp/`에는 이번 작업과 무관한 과거 산출물(csv, log, 예전 스크린샷 등)이 다수 남아 있으나 `.gitignore` 대상이라 커밋에 영향 없음 — 지시에 따라 결함으로 보지 않음(참고 사항으로만 기록).
