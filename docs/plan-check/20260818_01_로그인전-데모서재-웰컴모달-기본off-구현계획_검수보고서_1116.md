---
title: "로그인전-데모서재-웰컴모달-기본off-구현계획"
created: "2026-08-18 11:16"
session_id: "34fe3899-c854-4242-83b6-f40a8f41623a"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-my-bookstation/34fe3899-c854-4242-83b6-f40a8f41623a.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-08-18 11:16:04"
---

# Plan Check Reviewer Report

## Verdict
실행 가능

## Blocking Issues
없음

## Required Fixes
- **소스 코드 문자열 기반 회귀 테스트 제거 (과최적화 방지):** `tests/demo-library-data.test.mjs`에서 로그인 사용자 경로 무회귀를 소스 코드 문자열 단언(`viewType === 'card'`, `if (myLibraryBooks.length === 0)` 등)으로 확인하는 것은 극도로 깨지기 쉬운 불안정한(brittle) 테스트 패턴입니다. Prettier 포매팅이 동작하거나 `!length` 같은 사소한 문법 리팩터링만 발생해도 테스트가 실패하여 향후 유지보수에 큰 방해가 됩니다. 이 문자열 단언들을 삭제하고, Playwright UI 테스트에서 세션 스토리지나 쿠키를 모킹하여 로그인 뷰가 정상 렌더링되는지 확인하거나, 해당 회귀 검증은 완료 검수(done-check) 시 육안/AI 코드 리뷰로 대체하도록 계획을 수정해야 합니다.

## 참고 제안 (판정에 미반영)
- **개인정보(이메일) 필터링 조건 개선:** `tests/demo-library-data.test.mjs`에서 JSON 내 `@` 문자열 부재를 단언하는 로직은 도서 제목이나 출판사명에 `@`가 포함될 경우(예: "@ 20대") 거짓 양성(False Positive) 실패를 유발할 수 있습니다. 문자열 전체 검색 대신 `SelectedBook` 객체를 순회하며 특정 필드(`email`, `user_id` 등)가 제거되었는지 구조적으로 확인하는 것이 안전합니다.
- **SQL 쿼리 조작 주의:** 5-5 추출 절차에서 두 번째 쿼리의 `in (...)` 절에 첫 번째 쿼리 결과를 수동 또는 AI가 문자열 조작으로 주입해야 합니다. MCP 도구로 쿼리를 실행할 때 유효하지 않은 SQL이 되지 않도록 쉼표 구분 및 따옴표 처리에 유의해야 합니다.