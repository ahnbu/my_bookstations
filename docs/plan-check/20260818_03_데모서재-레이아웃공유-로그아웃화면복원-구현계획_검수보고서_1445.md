---
title: "데모서재-레이아웃공유-로그아웃화면복원-구현계획"
created: "2026-08-18 14:45"
session_id: "6ee47e9b-e84d-4cc1-ab7e-735c1ecdbe1e"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-my-bookstation/6ee47e9b-e84d-4cc1-ab7e-735c1ecdbe1e.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-08-18 14:45:41"
---

# Plan Check Reviewer Report

이 plan은 데모 서재 그리드 레이아웃의 구조적 분리를 통합하고, 비로그인 상태 안내 화면을 성공적으로 복원하여 본래 목적을 완벽하게 달성할 수 있습니다. 

접근법의 트레이드오프:
- **얻는 것**: `hasSignedIn` 키를 지우지 않는 전략은, 배포 직후 최초 접속하는 기존 사용자(`INITIAL_SESSION`)가 세션 만료 시 데모 화면으로 떨어지는 치명적 회귀 버그를 마이그레이션 스크립트 없이 완벽하게 방지합니다.
- **잃는 것**: 특정 기기/브라우저에서 한 번이라도 로그인한 사용자는 향후 로그아웃 상태에서도 예시 서재(데모)를 볼 수 없습니다. (다만 계획서 내에서 시크릿 창을 활용하기로 이미 합의 수용된 사항입니다)

## Verdict
실행 가능

## Blocking Issues
없음

## Required Fixes
1. **서술 예시 내 Placeholder 수정**: 4-2절 `onAuthStateChange` 코드 예시에 삽입된 `...` 마크는 서브에이전트가 코드를 교체할 때 기존 로직을 날려버리거나 텍스트 그대로 삽입할 위험이 있습니다. `// 기존 처리 로직 유지` 등 명시적인 주석으로 변경하십시오.
2. **테스트 검증 방식 확정(사람 판단/분기 제거)**: 8-1절에서 V-11 테스트를 "단위 검증으로 대체해도 된다. 이 경우 `tests/auth-hassignedin.test.mjs`를 신설하고"라고 열어두었습니다. exec-plan 등 자동화 에이전트 실행 시 스스로 판단해야 하므로 선후관계 충돌을 유발할 수 있습니다. UI 테스트 내 주입으로 갈지, 신설 단위 검증으로 갈지 하나의 지시로 고정하십시오.

## 참고 제안 (판정에 미반영)
- **테스트 셀렉터 주의**: `DemoLibrary`는 `MyLibrary`와 달리 책이 0권(`filteredBooks.length === 0`)일 때 `BookListContainer` 자체를 렌더링하지 않는 구조입니다. Wave 4 테스트 작성 시, 빈 목록 상황에서 `[data-testid="demo-book-list"]`가 아예 존재하지 않아 Locator 에러가 나지 않도록 단언문 작성에 유의하시기 바랍니다.
- 영구 데이터 조작 시 롤백 부담을 없앤 점(방치), 스크린샷 산출물이 `.gitignore`를 위반하지 않도록 격리한 점(`temp/`), Headless 브라우저 실행을 강제하여 GUI 간섭을 차단한 부분 등 자동화 친화적인 검증 설계가 매우 훌륭합니다.