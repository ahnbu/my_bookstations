---
title: "로그인전-데모서재-웰컴모달-기본off-구현계획"
created: "2026-08-18 11:13"
session_id: "34fe3899-c854-4242-83b6-f40a8f41623a"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-my-bookstation/34fe3899-c854-4242-83b6-f40a8f41623a.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-08-18 11:13:36"
---

# Plan Check Reviewer Report

본 계획은 정적 스냅샷 기반의 데모 화면 렌더링과 웰컴 모달 초기값 변경을 통해 비로그인 방문자에게 직관적인 온보딩 경험을 제공한다는 **목적을 충분히 달성할 수 있습니다.** 단, 아래 차단 이슈의 수정이 선행되어야 합니다.

## Verdict
수정 필요

## Blocking Issues
1. **`MyLibraryListItem` 상호작용 제어 설계 누락** / 근거: Phase 3 및 `components/MyLibraryListItem.tsx` 수정 범위 / 실행 전 막히는 이유: `DemoLibrary`에서 좋아요 등 클릭 시 알림만 띄우려면, `MyLibraryListItem` 내부의 스토어 액션이 실행되지 않도록 이벤트가 외부로 노출되거나 읽기 전용 플래그가 주입되어야 합니다. 현재 계획은 `tagsOverride` prop 추가만 명시하고 있어, 데모 도서 클릭 시 음수 ID를 가진 도서의 실제 스토어 액션이 트리거되어 에러가 발생하거나 로컬 상태가 오염됩니다.
2. **실행 불가 Placeholder 방치** / 근거: 5-5 두 번째 SQL 쿼리 (`${SELECTED_IDS}`) / 실행 전 막히는 이유: 쿼리 코드 블록 내에 치환되지 않은 Placeholder가 그대로 남아 있어, 자동화 도구가 이 코드 블록을 추출해 실행할 경우 SQL 문법 오류로 Phase 2 작업이 중단됩니다.
3. **소스 무회귀 검증 방법 결함** / 근거: Phase 4 검증 항목 (`git diff --stat`) / 실행 전 막히는 이유: `git diff --stat`은 변경된 파일명과 줄 수 통계만 반환합니다. 이를 기반으로 특정 렌더링 블록이나 함수 호출부 로직이 보존되었는지 내용적으로 단언(assert)할 수 없으므로, 검증 증거로 기능할 수 없어 테스트가 맹목적으로 실패하거나 무의미해집니다.

## Required Fixes
- `components/MyLibraryListItem.tsx` 수정 계획에 `isDemo` 플래그나 이벤트 콜백 prop 추가를 명시하여, 데모 모드일 경우 내부 스토어 로직 대신 외부에서 주입한 `setNotification`이 실행되도록 컴포넌트를 보완하세요.
- 5-5의 두 번째 SQL 쿼리에 있는 `${SELECTED_IDS}` 플레이스홀더를 코드 블록 밖으로 빼어 자연어로 설명하거나, AI에게 이전 조회 결과 기반으로 쿼리문을 동적 조립하여 실행하라는 명확한 행동 지시로 대체하세요.
- Phase 4의 회귀 검증 방법을 `git diff --stat` 대신 전체 내용 비교(`git diff components/MyLibrary.tsx`), AST 분석, 혹은 소스 코드 내 특정 문자열/정규식 보존 여부 단언으로 변경하세요.

## 참고 제안 (판정에 미반영)
- **트레이드오프 (얻는 것과 잃는 것)**: 본 계획은 DB 연동(네트워크 통신) 없이 정적 JSON을 활용하여 즉시 UI를 개선하고 백엔드 비용을 `0`으로 **얻는** 반면, 데이터가 스냅샷에 고정되어 시간이 흐르면 실제 운영 데이터(재고 상태 등)와 불일치가 발생할 수 있다는 데이터 정합성을 **잃습니다**. (UI 상 기준일 명시로 적절히 대응하였으나, 장기적으로는 갱신 스크립트가 필요할 수 있습니다).
- **과최적화 방어 우수**: 웰컴 모달 전역화를 위해 섣불리 Supabase 테이블과 API를 파지 않고 단순히 클라이언트의 기본값 fallback을 뒤집어 해결한 점, 불필요한 필드를 쳐내고 슬림 JSON으로 구성한 점은 비용과 복잡도를 낮춘 훌륭한 결정입니다.
- **번들 최적화 제안**: 초기 데모 데이터가 10권 분량의 슬림 JSON이라 크기가 미미하겠으나, 추후 권수가 늘어날 것을 대비해 `<DemoLibrary />` 내부에서 `data/demoLibrary.json`을 동적 임포트(`import()`)하여 비로그인 사용자에게만 로드하도록 최적화하는 방안도 고려해 볼 수 있습니다.