---
title: "로그인전-데모서재-웰컴모달-기본off-구현계획"
created: "2026-08-18 11:07"
session_id: "34fe3899-c854-4242-83b6-f40a8f41623a"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-my-bookstation/34fe3899-c854-4242-83b6-f40a8f41623a.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-08-18 11:07:58"
---

# Plan Check Reviewer Report

본 계획은 비로그인 방문자에게 서버 통신 없이(네트워크 비용 0) 즉각적인 데모 서재를 노출하여 온보딩을 돕는다는 핵심 목적을 훌륭히 달성할 수 있습니다. 
정적 데이터를 클라이언트에 하드코딩함에 따라 발생하는 '번들 크기 증가'와 '데이터 최신성 저하'라는 트레이드오프가 존재하나, 슬림 필드만 추출하고 스테디셀러 위주로 선정한다는 리스크 대응(8장)이 적절하게 설계되어 있어 합리적인 접근입니다.

## Verdict
수정 필요

## Blocking Issues
- **검증 자동화 불가 (사람 눈 의존)** / `Phase 1:129`, `Phase 4:155-156` / "시크릿 창에서 모달이 뜨지 않는지 확인", "기존 서재 화면이 그대로인지 육안 확인", "모바일 폭에서 데모 카드 레이아웃 확인" 등 수동(육안) 확인 항목이 계획되어 있습니다. 이대로 실행하면 브라우저 포커스를 빼앗기거나 후속 `done-check` 시 종료코드 및 로그 증거를 찾을 수 없어 자동 판정이 불가능합니다. 모두 Playwright를 이용한 headless 테스트로 대체해야 합니다.
- **실행 불가 Placeholder 잔존** / `Line 121: select ... where ul.id in (...);` / 그대로 실행해야 할 SQL 쿼리 조건절에 `...` placeholder가 남아 있어 쿼리 실행 시 반드시 실패합니다. 이전 단계의 결과를 받아 처리하는 동적 템플릿 변수(예: `${SELECTED_IDS}`) 또는 파이프라인 스크립트 형태로 구체화해야 합니다.

## Required Fixes
- Playwright 기반 UI 테스트 및 브라우저 검증 수행 시, 화면이 뜨지 않는 **headless 실행 모드**를 사용함을 명시하십시오.
- UI 테스트 리포트, 레이아웃 스크린샷 증거, 번들 사이즈(`pnpm build`) 측정 결과 등의 증거물(evidence)이 생성될 때 이를 어디에 저장하고 커밋에는 어떻게 반영할지(예: local-only, _docs 저장 등) 산출물 처리 방침을 닫아두십시오.
- `exec-plan`을 통해 안전하게 실행될 수 있도록 현재의 `## 6. 실행 단계`를 `## 실행 웨이브` 또는 `## 웨이브 구조` 블록으로 변경하고, 서브에이전트가 오해하지 않게 병렬 실행 가능한 작업과 선후 관계를 명시하십시오.

## 참고 제안 (판정에 미반영)
- **데이터 유효성 사전 검증**: `Line 117` 쿼리에 하드코딩된 이메일 계정(`byungwook.an@gmail.com`)이 운영 환경에 확실히 존재하고, 요구하는 재고 커버리지를 항상 만족하는지 사전에 가볍게 조회하는 방어 로직이 있으면 더욱 안전합니다.
- **사용자 개입 최소화**: `Phase 2`의 "선정 결과 사용자 확인" 절차는 자동화를 멈추게 만드는 병목입니다. 10권 선정 기준(6개 배지 커버리지 최소 1권)을 통과하는 즉시 사용자 대기 없이 JSON 스냅샷 생성을 진행하는 자동화 스크립트로 구성하면 실행 시간을 단축할 수 있습니다.