---
title: "데모서재-레이아웃공유-로그아웃화면복원-구현계획"
created: "2026-08-18 14:45"
session_id: "6ee47e9b-e84d-4cc1-ab7e-735c1ecdbe1e"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-my-bookstation/6ee47e9b-e84d-4cc1-ab7e-735c1ecdbe1e.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-08-18 14:45:26"
---

# Plan Check Reviewer Report

## Verdict
수정 필요

이 계획은 레이아웃 정본 통합과 데모/로그인 화면 분리라는 목적을 달성하기에 적절한 아키텍처를 제시하고 있으나, 기존 사용자 데이터를 누락하는 설계 결함과 자동화 검증을 방해하는 수동 검증 단계가 포함되어 있어 보완이 필요합니다.

## Blocking Issues
1. **기존 사용자 로그인 이력 마이그레이션 누락 (요구사항 누락)**
   - 근거: `4-2. 로그인 이력 판정`, `11. 리스크와 대응`, `8. 실행 단계 Wave 3`
   - 실행 전 막히는 이유: Supabase `onAuthStateChange`는 기존 세션 로드 시 `INITIAL_SESSION`, 토큰 갱신 시 `TOKEN_REFRESHED` 이벤트를 발생시킵니다. `SIGNED_IN`은 새롭게 로그인을 수행할 때만 발생하므로, 계획대로(`event === 'SIGNED_IN'`일 때만 저장) 실행하면 배포 시점에 이미 로그인되어 있는 기존 사용자들의 브라우저에는 `hasSignedIn`이 기록되지 않습니다. 이 상태에서 로그아웃하거나 세션이 만료되면 1번 목표(로그인 안내 화면 표시)에 실패하고 의도치 않게 데모 화면이 표시되어 기존 사용자 경험이 훼손됩니다.

2. **육안 확인을 요구하는 검증 항목 (자동화 판정 불가)**
   - 근거: `9-3. 최종 사용자 관점 확인` ("육안 확인하고 스크린샷을 남긴다")
   - 실행 전 막히는 이유: 작업 완료 후 `done-check`를 수행할 AI 에이전트는 브라우저 화면을 사람의 눈으로 '육안 확인'할 수 없습니다. 종료 코드나 로그로 성공 여부가 남지 않는 주관적 수동 검증 지시는 자동화된 완료 판정 파이프라인을 원천적으로 차단합니다.

## Required Fixes
1. **UI 테스트의 Headless 실행 명시**
   - 근거: `9-1. 통과 기준` 및 `8. 실행 단계 Wave 4`
   - 수정 내용: `tests/demo-library-ui.test.mjs` 실행 시 브라우저 창이 떠서 사용자 포커스를 뺏고 병행 작업을 끊지 않도록, `headless` 모드로 실행된다는 점(명령어 플래그 또는 Playwright 설정)을 명시해야 합니다.

2. **스크린샷 산출물 처리 규칙 누락**
   - 근거: `9-3. 최종 사용자 관점 확인`
   - 수정 내용: 육안 확인을 Playwright의 자동 스크린샷 캡처 테스트로 대체할 경우, 생성된 스크린샷 증거 파일을 Git 커밋에 포함할지, 아니면 `.gitignore` 등을 통해 `local-only`로 무시할지 처리 방침이 닫혀 있어야 합니다.

3. **서술 및 예시 내 Placeholder 제거**
   - 근거: `5-3` 표의 `new Date(...).getTime()`, `8. 실행 단계`의 `.sort(...)`
   - 수정 내용: 서술 내에 포함된 `...` placeholder가 실행 에이전트의 혼동을 유발할 수 있습니다. `new Date(a.pubDate).getTime()`과 같은 구체적인 코드나 "기존 구현 인자 유지"와 같은 명확한 지시어로 변경해야 합니다.

## 참고 제안 (판정에 미반영)
- **롤백 시 로컬 데이터(localStorage) 정리 명시:** `13. 롤백` 섹션에 `Wave 3`을 되돌릴 때 새로 추가된 `localStorage`의 `hasSignedIn` 키를 삭제(cleanup)할지, 애플리케이션에 무해하므로 그대로 방치할지 명시하면 더 완전한 롤백 계획이 됩니다.