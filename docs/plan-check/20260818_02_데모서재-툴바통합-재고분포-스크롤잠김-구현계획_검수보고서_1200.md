---
title: "데모서재-툴바통합-재고분포-스크롤잠김-구현계획"
created: "2026-08-18 12:00"
session_id: "34fe3899-c854-4242-83b6-f40a8f41623a"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-my-bookstation/34fe3899-c854-4242-83b6-f40a8f41623a.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-08-18 12:00:37"
---

# Plan Check Reviewer Report

## Verdict
수정 필요

## Blocking Issues
**1. 명령어 및 SQL 쿼리 내 실행 불가 플레이스홀더 포함**
- **지적**: 실행할 명령어와 쿼리 구문에 플레이스홀더가 그대로 노출되어 있음.
- **근거**: 9절 Wave 4의 명령어(`git worktree add "<baseline 경로>" HEAD` 등) 및 4-5절/9절 Wave 2의 3단계 쿼리문(`in (선정한_20개_ID를_쉼표로_나열)`)
- **실행 전 막히는 이유**: AI 작업 규칙상 실행할 명령어/경로에 플레이스홀더가 남아 있으면 차단 대상입니다. 특히 Windows 환경에서 `<`와 `>` 문자는 파일 경로에 사용할 수 없으며 파워쉘/CMD에서 리다이렉션 연산자로 해석되므로, `<baseline 경로>`가 포함된 명령을 그대로 복사하여 실행할 경우 구문 오류가 발생하고 반드시 실패합니다. AI가 알아서 치환할 것이라 가정하지 말고, 문서 상에서 `../baseline-worktree`와 같은 유효하고 고정된 명시적 경로로 모두 변경해야 합니다. 또한 SQL 쿼리 역시 플레이스홀더 대신, "1단계 조회 결과에서 추출한 20개 ID 배열을 SQL의 `IN (...)` 구문에 주입하여 실행한다"는 식의 스크립팅/절차적 서술로 수정해야 합니다.

## Required Fixes
**1. 설정 변수명 불일치 교정**
- 문서 내 5-1절에서는 웰컴 모달 상태를 `welcomeSettings.enabled`로 참조한다고 서술했으나, 9절 Wave 4의 UI 테스트 검증 조건에서는 `adminWelcomeMessageSettings.enabled=true`를 주입한다고 기재되어 있습니다. `useSettingsStore.ts`에 정의된 실제 변수명으로 계획 문서 전체를 통일하여 구현 및 테스트 과정의 혼선을 방지해야 합니다.

## 참고 제안 (판정에 미반영)
- **트레이드오프 확인 (로컬 상태 사용)**: 데모 서재를 위해 스토어를 쓰지 않고 로컬 상태를 활용하는 설계는 로그인 상태의 데이터 오염을 막는다는 큰 이점이 있습니다. 반대급부로 공용 컴포넌트인 `TagFilter`와 `MyLibraryToolbar`에 `tagCountsOverride` 같은 데모 전용 optional prop이 추가되어 컴포넌트 시그니처가 다소 복잡해집니다. 계획서에서 이를 인지하고 부작용을 최소화하는 방향으로 잘 방어하고 있으므로 훌륭한 설계 판단입니다.
- **태그 카운트의 동적/정적 여부 명시**: 5-3절 및 9절에서 `TagFilter`에 표시될 `tagCounts`를 런타임에 계산하여 주입한다고 하였습니다. 이때 주입하는 카운트가 '검색이나 좋아요 필터가 적용된 후의 동적 결과 수'인지, 아니면 '전체 20권 기준의 정적 총량'인지 명시해 두면 기존 `MyLibrary` 동작과의 완전한 일관성을 확보하는 데 도움이 될 것입니다.
- **번들 크기 삭제 시 안정성 (선택)**: 9절 Wave 4의 `(Get-Item "<baseline 경로>\node_modules").Delete()`는 타겟을 건드리지 않고 Junction만 지우는 유효한 파워쉘 명령어이나, 파워쉘 버전 및 환경에 따라 예기치 않게 타겟 디렉터리에 간섭할 가능성을 완전히 배제하려면 기본 `cmd /c rmdir ..\baseline-worktree\node_modules`를 사용하는 것이 가장 원시적이고 확실한 안전장치가 될 수 있습니다.