[1mdiff --git a/.claude/settings.local.json b/.claude/settings.local.json[m
[1mindex 43faf91..1fdf981 100644[m
[1m--- a/.claude/settings.local.json[m
[1m+++ b/.claude/settings.local.json[m
[36m@@ -20,7 +20,11 @@[m
       "Bash(curl:*)",[m
       "Bash(touch:*)",[m
       "Bash(npx wrangler dev:*)",[m
[31m-      "Bash(find:*)"[m
[32m+[m[32m      "Bash(find:*)",[m
[32m+[m[32m      "Bash(where claude)",[m
[32m+[m[32m      "Bash(claude config list)",[m
[32m+[m[32m      "Bash(claude mcp:*)",[m
[32m+[m[32m      "Bash(uvx:*)"[m
     ],[m
     "deny": [],[m
     "defaultMode": "plan"[m
[1mdiff --git a/.serena/project.yml b/.serena/project.yml[m
[1mnew file mode 100644[m
[1mindex 0000000..727a9ec[m
[1m--- /dev/null[m
[1m+++ b/.serena/project.yml[m
[36m@@ -0,0 +1,68 @@[m
[32m+[m[32m# language of the project (csharp, python, rust, java, typescript, go, cpp, or ruby)[m
[32m+[m[32m#  * For C, use cpp[m
[32m+[m[32m#  * For JavaScript, use typescript[m
[32m+[m[32m# Special requirements:[m
[32m+[m[32m#  * csharp: Requires the presence of a .sln file in the project folder.[m
[32m+[m[32mlanguage: typescript[m
[32m+[m
[32m+[m[32m# whether to use the project's gitignore file to ignore files[m
[32m+[m[32m# Added on 2025-04-07[m
[32m+[m[32mignore_all_files_in_gitignore: true[m
[32m+[m[32m# list of additional paths to ignore[m
[32m+[m[32m# same syntax as gitignore, so you can use * and **[m
[32m+[m[32m# Was previously called `ignored_dirs`, please update your config if you are using that.[m
[32m+[m[32m# Added (renamed)on 2025-04-07[m
[32m+[m[32mignored_paths: [][m
[32m+[m
[32m+[m[32m# whether the project is in read-only mode[m
[32m+[m[32m# If set to true, all editing tools will be disabled and attempts to use them will result in an error[m
[32m+[m[32m# Added on 2025-04-18[m
[32m+[m[32mread_only: false[m
[32m+[m
[32m+[m
[32m+[m[32m# list of tool names to exclude. We recommend not excluding any tools, see the readme for more details.[m
[32m+[m[32m# Below is the complete list of tools for convenience.[m
[32m+[m[32m# To make sure you have the latest list of tools, and to view their descriptions,[m[41m [m
[32m+[m[32m# execute `uv run scripts/print_tool_overview.py`.[m
[32m+[m[32m#[m
[32m+[m[32m#  * `activate_project`: Activates a project by name.[m
[32m+[m[32m#  * `check_onboarding_performed`: Checks whether project onboarding was already performed.[m
[32m+[m[32m#  * `create_text_file`: Creates/overwrites a file in the project directory.[m
[32m+[m[32m#  * `delete_lines`: Deletes a range of lines within a file.[m
[32m+[m[32m#  * `delete_memory`: Deletes a memory from Serena's project-specific memory store.[m
[32m+[m[32m#  * `execute_shell_command`: Executes a shell command.[m
[32m+[m[32m#  * `find_referencing_code_snippets`: Finds code snippets in which the symbol at the given location is referenced.[m
[32m+[m[32m#  * `find_referencing_symbols`: Finds symbols that reference the symbol at the given location (optionally filtered by type).[m
[32m+[m[32m#  * `find_symbol`: Performs a global (or local) search for symbols with/containing a given name/substring (optionally filtered by type).[m
[32m+[m[32m#  * `get_current_config`: Prints the current configuration of the agent, including the active and available projects, tools, contexts, and modes.[m
[32m+[m[32m#  * `get_symbols_overview`: Gets an overview of the top-level symbols defined in a given file.[m
[32m+[m[32m#  * `initial_instructions`: Gets the initial instructions for the current project.[m
[32m+[m[32m#     Should only be used in settings where the system prompt cannot be set,[m
[32m+[m[32m#     e.g. in clients you have no control over, like Claude Desktop.[m
[32m+[m[32m#  * `insert_after_symbol`: Inserts content after the end of the definition of a given symbol.[m
[32m+[m[32m#  * `insert_at_line`: Inserts content at a given line in a file.[m
[32m+[m[32m#  * `insert_before_symbol`: Inserts content before the beginning of the definition of a given symbol.[m
[32m+[m[32m#  * `list_dir`: Lists files and directories in the given directory (optionally with recursion).[m
[32m+[m[32m#  * `list_memories`: Lists memories in Serena's project-specific memory store.[m
[32m+[m[32m#  * `onboarding`: Performs onboarding (identifying the project structure and essential tasks, e.g. for testing or building).[m
[32m+[m[32m#  * `prepare_for_new_conversation`: Provides instructions for preparing for a new conversation (in order to continue with the necessary context).[m
[32m+[m[32m#  * `read_file`: Reads a file within the project directory.[m
[32m+[m[32m#  * `read_memory`: Reads the memory with the given name from Serena's project-specific memory store.[m
[32m+[m[32m#  * `remove_project`: Removes a project from the Serena configuration.[m
[32m+[m[32m#  * `replace_lines`: Replaces a range of lines within a file with new content.[m
[32m+[m[32m#  * `replace_symbol_body`: Replaces the full definition of a symbol.[m
[32m+[m[32m#  * `restart_language_server`: Restarts the language server, may be necessary when edits not through Serena happen.[m
[32m+[m[32m#  * `search_for_pattern`: Performs a search for a pattern in the project.[m
[32m+[m[32m#  * `summarize_changes`: Provides instructions for summarizing the changes made to the codebase.[m
[32m+[m[32m#  * `switch_modes`: Activates modes by providing a list of their names[m
[32m+[m[32m#  * `think_about_collected_information`: Thinking tool for pondering the completeness of collected information.[m
[32m+[m[32m#  * `think_about_task_adherence`: Thinking tool for determining whether the agent is still on track with the current task.[m
[32m+[m[32m#  * `think_about_whether_you_are_done`: Thinking tool for determining whether the task is truly completed.[m
[32m+[m[32m#  * `write_memory`: Writes a named memory (for future reference) to Serena's project-specific memory store.[m
[32m+[m[32mexcluded_tools: [][m
[32m+[m
[32m+[m[32m# initial prompt for the project. It will always be given to the LLM upon activating the project[m
[32m+[m[32m# (contrary to the memories, which are loaded on demand).[m
[32m+[m[32minitial_prompt: ""[m
[32m+[m
[32m+[m[32mproject_name: "my_bookstation"[m
[1mdiff --git a/components/APITest.tsx b/components/APITest.tsx[m
[1mindex e518283..8ac1665 100644[m
[1m--- a/components/APITest.tsx[m
[1m+++ b/components/APITest.tsx[m
[36m@@ -21,7 +21,13 @@[m [mconst APITest: React.FC = () => {[m
   [m
   // Store에서 검색 결과와 선택된 책 가져오기[m
   const { searchResults, selectedBook } = useBookStore();[m
[31m-  const { isBookModalOpen, closeBookModal } = useUIStore();[m
[32m+[m[32m  const { isBookModalOpen, closeBookModal, setAPITestMode } = useUIStore();[m
[32m+[m
[32m+[m[32m  // API 테스트 모드 활성화/비활성화[m
[32m+[m[32m  useEffect(() => {[m
[32m+[m[32m    setAPITestMode(true);[m
[32m+[m[32m    return () => setAPITestMode(false);[m
[32m+[m[32m  }, [setAPITestMode]);[m
 [m
   // 복사 기능 함수[m
   const copyToClipboard = async (text: string, label: string) => {[m
[36m@@ -80,7 +86,7 @@[m [mconst APITest: React.FC = () => {[m
   const processedTitle = title ? processBookTitle(title) : '';[m
 [m
   return ([m
[31m-    <div className="mt-12 animate-fade-in">[m
[32m+[m[32m    <div className="mt-12 animate-fade-in api-test-container">[m
       <h2 className="text-3xl font-bold text-white mb-6">API 테스트</h2>[m
       [m
       <div className="bg-gray-800 rounded-lg shadow-xl p-6">[m
[36m@@ -95,7 +101,7 @@[m [mconst APITest: React.FC = () => {[m
                 {searchResults.map((book) => ([m
                   <div [m
                     key={book.isbn13}[m
[31m-                    onClick={() => useBookStore.getState().selectBook(book)}[m
[32m+[m[32m                    onClick={() => useBookStore.getState().selectBook(book, { scroll: false })}[m
                  