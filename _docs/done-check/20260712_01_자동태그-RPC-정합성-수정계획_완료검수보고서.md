---
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-07-12 14:47:28"
---

# Heavy Mode 완료 검수 보고서

## 최종 판정

**Pass** — 기존 production build chunk-size advisory 외 신규 이슈 없음.

## 1. 계획 요구사항

- 확정 범위의 SQL, 운영 스크립트, 테스트, README, DEVELOPMENT, 계획·검수 문서가 구현됐습니다.
- 프론트엔드, 구형 1인자 RPC, migrations/project link, Worker는 변경하지 않았습니다.
- 운영 데이터와 Chrome UI 결과가 계획 기준값과 일치합니다.

## 2. 운영 스크립트와 보안

- 출력은 count, boolean, 함수 속성으로 제한되며 token, user UUID, tag ID를 노출하지 않습니다.
- .env.local의 Supabase project ref를 검증합니다.
- Inspect, Apply, Verify, Rollback을 제공합니다.
- Apply 전 백업을 만들고 기존 백업 덮어쓰기를 거부합니다.
- 적용 또는 검증 실패 시 같은 PowerShell 프로세스가 별도 Management API 요청으로 백업 SQL을 재적용하고 exit 1로 종료합니다.

## 3. SQL 계약

- get_tag_counts_for_user()는 customTags와 autoTags를 병합하고 책별 중복을 제거합니다.
- get_books_by_tags(text[], boolean)는 병합 태그 AND 조건과 좋아요 조건을 유지합니다.
- 2인자 함수는 필수 boolean, STABLE, SECURITY INVOKER 계약입니다.

## 4. 구형 1인자 함수

- 적용 SQL에 구형 get_books_by_tags(text[]) 정의가 없습니다.
- Apply 전후 SHA-256 비교가 legacy 함수 불변을 검증했습니다.

## 5. 테스트와 문서

- SQL 계약 테스트와 기존 자동태그 테스트가 통과했습니다.
- README와 DEVELOPMENT가 수동 customTags, 자동 autoTags, 병합 RPC, 독립 SQL의 운영 적용 경계를 설명합니다.

## 6. Critical / Advisory

- Critical: 없음
- Advisory: 기존 Vite 500 kB chunk-size 경고. 이번 RPC/SQL 변경과 무관합니다.

## 독립 확인 메모

agy 원문의 “동일한 Management API 세션 내 자동 롤백” 표현은 정확하지 않습니다. 실제 구현은 같은 PowerShell 프로세스의 catch 블록이 별도 Management API 요청으로 백업 SQL을 재실행합니다. 백업 원문과 자동 롤백 경로가 있으므로 Gate 판정에는 영향이 없습니다.
