---
title: "데모모드-안내띠-상단분리-구현계획"
created: "2026-08-18 15:16"
session_id: "6ee47e9b-e84d-4cc1-ab7e-735c1ecdbe1e"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-my-bookstation/6ee47e9b-e84d-4cc1-ab7e-735c1ecdbe1e.jsonl"
ai: "claude"
model: "independent-lite-reviewer"
performed_at: "2026-08-18 15:16:23"
---

# Plan Check Lite Reviewer Report

## Verdict
수정 필요

## Blocking Issues
없음 (실행 자체를 막는 항목은 없음 — 아래 Required Fixes는 목적 달성도와 완료기준의 증거 판정 가능성에 영향을 준다)

## Required Fixes

1. **파일:라인 인용 오류 2건**
   - `MyLibrary.tsx:792-798`(4-1절) — 실제로 `if (!session)` 분기는 792~800줄(`return <DemoLibrary />`가 799줄, 닫는 `}`가 800줄)이다. 792-798로는 return문과 닫는 괄호가 인용 범위 밖으로 빠진다.
   - `tests/demo-library-ui.test.mjs:76`(9-1절 "headless 강제") — 실제 `chromium.launch({ executablePath, headless: true })`는 86줄이다. 76줄은 `spawn(...)` 호출의 닫는 괄호다.
   나머지 인용(`index.css:28`, `BookDetails.tsx:272-276`, `WelcomeModal.tsx:89`, `AuthModal.tsx:114`, `Notification.tsx:48`, `.gitignore:52`, `DemoLibrary.tsx:112-115`, `Header.tsx`의 `flex...mb-10`, 커밋 `3f52dcf`)는 모두 실제 코드와 일치함을 확인했다.

2. **계획 내부 모순 — CTA 제외 근거가 코드 사실과 불일치**
   Section 2 확정 원칙 "메타 설명과 서비스 본체를 섞지 않는다"와 Section 3 제외("CTA는 화면에 대한 설명이 아니라 제품 전환 동선")가 충돌한다. 실제로 유지되는 하단 텍스트는 `DemoLibrary.tsx:187-189`의 `위 목록은 예시이며, 재고는 {DEMO_SNAPSHOT_DATE} 기준입니다`로, 이는 버튼(전환 동선)이 아니라 화면에 대한 메타 설명이다. 이 계획이 해결하려는 근본 문제(1-2절)와 정확히 같은 종류의 문장이 서비스 본체에 남는다. V-14는 "예시 서재"·"미리보기" 정확 문자열만 검사하므로 이 문구는 게이트를 통과하고, 목적 미달성이 자동 검증에서 드러나지 않는다. 해당 문구를 배너로 옮기거나, 제외 근거를 코드 사실에 맞게 재작성해야 한다.

3. **번들 "+3KB 이내" 기준에 baseline 명시 없음**
   Wave 5는 사후 측정(`du -b dist/assets/*.js`)만 지시하고 비교 대상 이전 값이 어디서 오는지 적혀 있지 않다. 선행 계획 03의 실행결과(634,308 bytes)를 baseline으로 쓰는 것으로 추정되나 이 문서에 명문화되어 있지 않아, done-check가 "+3KB 이내"를 증거 기반으로 판정하려면 별도로 baseline을 찾아야 한다. baseline 수치를 이 계획에 직접 박거나, "구현 전 동일 절차로 baseline 측정" 단계를 Wave 1 이전에 추가해야 한다.

4. **sticky 배너-토스트 물리적 겹침 케이스 누락**
   충돌표(4-2절, 12절)는 배너(`z-40`)와 토스트(`Notification.tsx:48`, `z-[100]`)의 z-index 순서만 다룬다. 토스트는 `fixed top-5 right-5`로 뷰포트 상단 20px 지점에 뜨는데, 배너는 전체폭 `sticky top-0`이며 높이가 대략 36~40px(`py-2` + `text-sm`)이라 초기 스크롤 위치에서 두 요소가 물리적으로 겹칠 수 있다. z-index 순서상 토스트가 위에 그려져 가려지지는 않겠지만, 배너 우측 텍스트 일부가 토스트에 시각적으로 덮이는 케이스는 검토·9-3 스크린샷 목록(4종) 어디에도 포함되지 않았다.

5. **(경미) `MyLibrary.tsx:421`의 `session` destructure 정리 누락 가능성**
   `session`은 421줄(선언)과 792줄(분기 조건)에서만 쓰인다. Wave 1에서 `if (!session)`을 `useDemoMode()`로 교체하면 421줄의 `const { session } = useAuthStore();`가 미사용 변수로 남을 수 있다. `tsconfig.json`에 `noUnusedLocals`가 없어 `tsc --noEmit` 게이트는 이 상태로도 통과하므로 죽은 코드가 남을 위험이 있다. 9-4절 diff 허용 범위(2종)에 이 정리 항목을 포함할지 명시가 필요하다.

## 참고 (Blocking은 아니지만 검수 관점에 대한 답)
- V-12~V-15는 실제 코드 구조(`data-testid`, `closest('main')`, `getBoundingClientRect`, `hasSignedIn` 주입 패턴)와 일치하며 Playwright로 그대로 구현 가능하다.
- 9-3은 사람이 스크린샷을 열어 확인하는 게이트이나, 9-1(자동 통과 기준) 표에는 포함되어 있지 않아 자동 판정 파이프라인 자체를 막지는 않는다. 다만 `temp/`가 gitignore 대상이라 스크린샷이 로컬에만 남으므로, 이후 별도 세션에서 done-check가 열어보려면 같은 작업 디렉터리에 파일이 보존돼 있어야 한다는 전제가 암묵적이다.
- 범위 통제는 전반적으로 양호하다 — 신규/수정 대상 파일이 명확하고, `BookListContainer`/`useGridColumns`/`librarySort` 등 공유 코드와 Worker를 명시적으로 제외했다.
