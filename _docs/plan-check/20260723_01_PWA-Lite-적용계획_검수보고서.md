---
title: "PWA-Lite-적용계획"
created: "2026-07-23 23:13"
session_id: "61de158b-0253-4b61-8239-7265de7b0b8f"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-my-bookstation/61de158b-0253-4b61-8239-7265de7b0b8f.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-07-23 23:13:51"
---

# Plan Check Reviewer Report

## Verdict
수정 필요

이 plan은 PWA Lite(홈 화면 아이콘 + standalone 앱 느낌)의 목적을 달성할 수 있는 적절한 접근법(manifest 추가, meta 태그 삽입, Vite의 `public/` 폴더를 활용한 아이콘 정적 서빙)을 취하고 있어 성공 가능성이 높습니다. Vercel 배포 시에도 `dist` 폴더 루트에서 정상적으로 서빙됩니다. 그러나 실행 단계 및 사후 `done-check` 검증 시에 막히는 이슈들이 존재하여 수정이 필요합니다.

## Blocking Issues
1. **[지적] 브라우저/UI 검증 자동화 누락**
   - **근거**: `작업 4: 로컬 동작 검증`의 검증 방법에 "브라우저 콘솔 에러 로그에 아이콘·manifest 관련 오류가 없다" 및 "dev 서버 기동 후 응답이 200이다"라고 명시되어 있으나, headless 실행과 종료 코드 및 로그 증거 확보 방법이 명시되지 않았습니다.
   - **실행 전 막히는 이유**: 나중에 `done-check`를 수행하는 AI가 증거 로그가 없어 브라우저를 수동으로 띄우려 시도하다가 실패하거나 검증 불가 판정을 내리게 됩니다.

2. **[지적] 존재하지 않는 명령어 사용 (`security-gate`)**
   - **근거**: `작업 1: 앱 아이콘 PNG 생성`의 실행 방법 1번에 `security-gate install-vet --target sharp`라고 기재되어 있습니다.
   - **실행 전 막히는 이유**: `security-gate`는 쉘의 PATH에 등록된 명령어가 아니므로 그대로 실행 시 `CommandNotFound` 에러가 발생하여 작업이 초반에 중단됩니다. 전역 규칙에 정의된 절대경로 스크립트로 호출해야 합니다.

3. **[지적] 육안 확인 항목은 자동 검증 불가**
   - **근거**: `작업 1`의 검증 항목 중 "미리보기(이미지 열람)로 A안 디자인(인디고 배경 + 흰 심볼)이 육안 확인된다."
   - **실행 전 막히는 이유**: `done-check` 리뷰어는 AI 에이전트이므로 UI를 통한 주관적인 육안 확인이나 이미지 디자인 검수를 스스로 할 수 없습니다.

## Required Fixes
계획 문서(`D:/vibe-coding/my_bookstation/docs/20260723_01_PWA-Lite-적용계획.md`)에 다음 사항을 반영하십시오.

1. **검증 자동화 (작업 4)**: 검증 방법을 수정하여, `playwright-cli` 또는 `node C:\Users\ahnbu\.claude\skills\_shared\hidden-browser-verify-runner.mjs` 등을 활용해 로컬 서버 구동 → 브라우저 접속 → Network 패널에서 `/manifest.webmanifest`, `/icon-192.png` 200 응답 확인 → Console 에러 0개 확인 후 종료되는 **스크립트 실행 및 로그 증거 확보** 지침을 명시하십시오.
2. **보안 게이트 경로 수정 (작업 1)**: `security-gate` 명령어를 `node C:\Users\ahnbu\.claude\skills\_shared\security-gate.mjs install-vet --target sharp` 로 절대경로를 사용하도록 수정하십시오.
3. **육안 검증 제거 (작업 1)**: "육안 확인된다" 검증 항목을 삭제하거나 **(검증 불가)** 로 명시하고, `file` 도구나 node.js의 `image-size` 패키지 등을 통해 생성된 PNG의 파일 존재 및 크기 해상도(192x192, 512x512) 검증만으로 완료 기준을 객관화하십시오. 아울러 SVG 정의를 스크립트로 구성할 때 필요한 대략적인 로직(색상 코드 및 사이즈 설정 등)을 명확하게 지정하십시오.
