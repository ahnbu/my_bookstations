---
title: 자동태그 RPC 정합성 수정 Implementation Plan
created: 2026-07-12 14:28
session_id: codex:019f54b5-a934-7b53-8e1b-82cc3329a720
session_path: C:/Users/ahnbu/.codex/sessions/2026/07/12/rollout-2026-07-12T14-03-45-019f54b5-a934-7b53-8e1b-82cc3329a720.jsonl
ai: codex
---

# 자동태그 RPC 정합성 수정 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [x]`) syntax for tracking.

**Goal:** 운영 Supabase의 태그 카운트와 현재 앱이 사용하는 태그 필터가 `customTags + autoTags`를 함께 반영하여 `클로드` 14권을 정확히 표시·조회한다.

**Architecture:** 프론트엔드와 자동태그 저장 로직은 변경하지 않는다. 저장소의 기존 병합 SQL을 현재 운영 함수의 권한·최적화 속성에 맞게 최소 보정한 뒤, 사용자 승인 후 운영 Supabase의 `get_tag_counts_for_user()`와 `get_books_by_tags(text[], boolean)`에만 적용한다. 적용 전후 함수 정의와 실데이터 기준값을 비교하고 README와 개발문서의 태그 데이터 설명만 동기화한다.

**Tech Stack:** PostgreSQL 17, Supabase RPC/PostgREST, Node.js 24, React 19, TypeScript 5, `node:test`

---

## 확정 범위

### 포함

- `supabase/20260705_auto_tags_rpc.sql`의 2인자 필터 함수가 기존 운영 계약인 `STABLE + SECURITY INVOKER`와 필수 boolean 인자를 유지한다.
- 운영 `get_tag_counts_for_user()`가 책별 `customTags + autoTags`를 중복 없이 집계한다.
- 운영 `get_books_by_tags(text[], boolean)`가 병합 태그에 대해 기존 AND 조건과 좋아요 조건을 유지한다.
- 운영 반영 전 사용자 승인을 받으며, 반영 후 실제 계정 기준 카운트·필터·화면을 검증한다.
- `README.md`와 `docs/DEVELOPMENT.md`의 태그 저장·RPC·SQL 위치 설명을 최소 수정한다.
- Windows Credential Manager의 기존 Supabase CLI 인증을 사용하는 운영 스크립트가 조회·백업·적용·검증·자동 롤백을 같은 계약으로 수행한다.

### 제외

- Supabase 프로젝트 링크 설정
- `supabase/migrations/` 도입 또는 기존 SQL의 migration 이전
- 구형 `get_books_by_tags(text[])` 오버로드의 변경·삭제
- `recalculateAutoTagsForAllBooks()` 및 설정 화면의 `0권 변경` 문구 변경
- 프론트엔드 컴포넌트·스토어 로직 변경
- 커밋·푸시·배포 자동화 체계 변경

## 사전 확정 기준값

- 대상 계정: 전체 660권
- `클로드`: `customTags` 0권, `autoTags` 14권, 병합 14권
- 현재 운영 2인자 필터 결과: 0권
- 기대 결과: `클로드` 14권
- `클로드 + 좋아요`: 4권
- `클로드 + AI`: 14권
- `클로드 + AI + 좋아요`: 4권
- 회귀 대조군 `개인`: 카운트 355권, 필터 355권
- `customTags`와 `autoTags`의 동일 태그 중복 보유: 235권

## 파일 구조

- Create: `tests/autoTagRpcSql.test.mjs` — 병합 SQL과 2인자 함수 속성의 저장소 계약을 검증한다.
- Create: `supabase/manage-auto-tag-rpc.ps1` — 운영 함수 조회·백업·적용·검증·롤백을 수행한다.
- Modify: `supabase/20260705_auto_tags_rpc.sql` — 병합 로직은 유지하고 2인자 함수의 기존 운영 속성을 보존한다.
- Modify: `README.md` — 태그 데이터와 RPC의 빠른 참조를 현재 모델에 맞춘다.
- Modify: `docs/DEVELOPMENT.md` — 수동·자동 태그 분리, RPC 병합, SQL 정본 위치를 상세히 기록한다.
- Modify: `_docs/20260712_01_자동태그-RPC-정합성-수정계획.md` — 실행 증거와 완료 상태만 반영한다.
- Create: `_docs/plan-check/*자동태그-RPC-정합성-수정계획*검수보고서*.md` — 재검수를 포함한 heavy 계획검수 원문을 보존한다.
- Create: `_docs/done-check/*자동태그-RPC-정합성-수정계획*완료검수보고서.md` — heavy 완료검수 원문을 보존한다.
- Local-only: `supabase/.temp/auto_tag_rpc_preapply.sql` — 운영 반영 전 함수 원문 백업이며 커밋하지 않는다.

---

### Task 1: 저장소 SQL 계약이 테스트로 고정된다

**Files:**
- Create: `tests/autoTagRpcSql.test.mjs`
- Modify: `supabase/20260705_auto_tags_rpc.sql:20-47`

**완료 기준:** `node tests/autoTagRpcSql.test.mjs`가 `auto-tag RPC SQL tests passed`를 출력하고, 2인자 필터 함수가 `customTags + autoTags`, AND 조건, 좋아요 조건, 필수 boolean 인자, `STABLE + SECURITY INVOKER`를 모두 보존한다.

- [x] **Step 1: 현재 SQL이 운영 함수 속성을 바꾸는 문제를 재현하는 테스트가 존재한다**

`tests/autoTagRpcSql.test.mjs`를 다음 내용으로 생성한다.

```javascript
import assert from 'node:assert/strict';
import { readFileSync } from 'node:fs';

const sql = readFileSync('supabase/20260705_auto_tags_rpc.sql', 'utf8');

const countFunction = sql.match(
  /create or replace function public\.get_tag_counts_for_user\(\)[\s\S]*?\$\$;/i,
)?.[0];
const filterFunction = sql.match(
  /create or replace function public\.get_books_by_tags\([\s\S]*?filter_by_favorites[\s\S]*?\$\$;/i,
)?.[0];

assert.ok(countFunction, 'get_tag_counts_for_user() definition must exist');
assert.ok(filterFunction, 'get_books_by_tags(text[], boolean) definition must exist');

assert.match(countFunction, /book_data->'customTags'/);
assert.match(countFunction, /book_data->'autoTags'/);
assert.match(countFunction, /select distinct ul\.id, tag_id/i);

assert.match(filterFunction, /book_data->'customTags'/);
assert.match(filterFunction, /book_data->'autoTags'/);
assert.match(filterFunction, /tags_to_filter\s*<@/i);
assert.match(filterFunction, /filter_by_favorites\s+is\s+not\s+true/i);
assert.doesNotMatch(filterFunction, /filter_by_favorites\s+boolean\s+default/i);
assert.match(filterFunction, /\bstable\b/i);
assert.match(filterFunction, /security\s+invoker/i);
assert.doesNotMatch(filterFunction, /security\s+definer/i);

console.log('auto-tag RPC SQL tests passed');
```

- [x] **Step 2: 수정 전 테스트가 권한·volatility 계약 차이로 실패한다**

Run: `node tests/autoTagRpcSql.test.mjs`

Expected: FAIL. 2인자 함수에 `default false`가 있고, `stable`과 `security invoker`가 없으며, `security definer`가 있으므로 assertion error가 발생한다.

- [x] **Step 3: 2인자 함수의 병합 로직은 유지되고 기존 운영 속성만 보존된다**

`supabase/20260705_auto_tags_rpc.sql`의 2인자 함수 속성만 다음 diff처럼 수정한다. 카운트 함수와 필터 조건은 변경하지 않는다.

```diff
 create or replace function public.get_books_by_tags(
   tags_to_filter text[],
-  filter_by_favorites boolean default false
+  filter_by_favorites boolean
 )
 returns setof public.user_library
 language sql
-security definer
+stable
+security invoker
 set search_path = public
```

- [x] **Step 4: SQL 계약 테스트가 통과한다**

Run: `node tests/autoTagRpcSql.test.mjs`

Expected: PASS and output contains `auto-tag RPC SQL tests passed`.

- [x] **Step 5: 기존 자동태그 순수함수 회귀 테스트가 통과한다**

Run: `node tests/autoTagRules.test.mjs`

Expected: PASS and output contains `autoTagRules tests passed`.

---

### Task 2: 태그 데이터와 SQL 위치 문서가 현재 구현과 일치한다

**Files:**
- Modify: `README.md:167-173`
- Modify: `docs/DEVELOPMENT.md:256-288`

**완료 기준:** README는 빠른 참조만 제공하고, DEVELOPMENT는 `customTags`, `autoTags`, 두 RPC, 독립 SQL 파일의 역할을 정확히 설명한다. 프로젝트 링크나 migration 도입을 완료사항처럼 기록하지 않는다.

- [x] **Step 1: README의 태그 빠른 참조가 수동·자동 저장 위치를 구분한다**

`README.md`의 태그 데이터 안내를 다음 의미로 최소 수정한다.

- 수동·레거시 태그는 `book_data.customTags[]`에 저장된다.
- 자동 규칙 결과는 `book_data.autoTags[]`에 저장된다.
- 화면·카운트·필터는 두 배열을 병합해 사용한다.
- 운영 RPC 수정 SQL의 저장소 정본은 `supabase/20260705_auto_tags_rpc.sql`이다.

- [x] **Step 2: DEVELOPMENT의 잘못된 SQL 부재 설명이 현재 상태로 교체된다**

`docs/DEVELOPMENT.md`의 태그 데이터 표와 설명을 다음 계약으로 수정한다.

- `tagSettings.tags[]`: 태그 정의
- `tagSettings.autoTagRules[]`: 계정별 자동 규칙
- `book_data.customTags[]`: 수동·레거시 태그 ID
- `book_data.autoTags[]`: 자동 규칙 결과 태그 ID
- `get_tag_counts_for_user()`: 두 배열의 책별 중복 제거 집계
- `get_books_by_tags(text[], boolean)`: 두 배열 병합 기준 AND 필터와 좋아요 필터
- SQL 파일은 저장소에 존재하지만 운영 적용은 별도 승인·검증이 필요함

- [x] **Step 3: 문서에 제외 범위가 잘못 포함되지 않았다**

Run: `rg -n "project link|supabase link|migrations/|migration 이전" README.md docs/DEVELOPMENT.md`

Expected: 이번 수정의 완료사항으로 프로젝트 링크나 migration 도입을 설명하는 신규 문장이 없다.

- [x] **Step 4: 태그 데이터 핵심 용어가 두 문서에 존재한다**

Run: `rg -n "customTags|autoTags|get_tag_counts_for_user|get_books_by_tags|20260705_auto_tags_rpc.sql" README.md docs/DEVELOPMENT.md`

Expected: 두 문서가 수동·자동 태그와 RPC 병합 계약을 함께 설명한다.

---

### Task 3: 운영 반영이 승인·백업·검증 절차를 통과한다

**Files:**
- Create: `supabase/manage-auto-tag-rpc.ps1`
- Read: `supabase/20260705_auto_tags_rpc.sql`
- Modify: `_docs/20260712_01_자동태그-RPC-정합성-수정계획.md`의 실행 증거

**완료 기준:** 사용자 승인 후 운영 스크립트가 두 대상 함수만 원자적으로 갱신하고, 함수 정의·권한 속성·실데이터 결과가 기준값과 일치한다. 실패 시 사전 원문이 자동 복구되며, 프로젝트 링크와 migration은 생성되지 않는다.

- [x] **Step 1: 배포 전 보안 점검이 통과한다**

Run: `node C:/Users/ahnbu/.claude/skills/_shared/security-gate.mjs deploy-vet --target D:/vibe-coding/my_bookstation/supabase/20260705_auto_tags_rpc.sql`

Expected: secret·credential·외부 전송 위험이 없다는 통과 결과가 출력된다. 위험이 감지되면 운영 반영을 중단하고 사용자 확인을 받는다.

- [x] **Step 2: 운영 실행 스크립트가 자격증명을 노출하지 않고 모든 DB 단계를 제공한다**

`supabase/manage-auto-tag-rpc.ps1`은 다음 계약으로 구현한다.

- 파라미터: `-Action Inspect|Apply|Verify|Rollback`, `-ProjectRef ugzruzaywohbynjzjesm`, `-SqlPath`, `-BackupPath`
- 인증: 현재 프로세스의 `SUPABASE_ACCESS_TOKEN`을 우선 사용하고, 없으면 Windows Credential Manager의 `Supabase CLI:supabase` generic credential을 Win32 `CredReadW`로 읽는다. 두 경로 모두 값은 출력하지 않는다.
- 보안: PAT, API key, user UUID, tag ID 원문을 stdout·stderr·파일에 기록하지 않는다.
- 프로젝트 검증: `.env.local`의 `VITE_SUPABASE_URL` project ref가 `-ProjectRef`와 다르면 실행을 중단한다.
- Management API: `POST https://api.supabase.com/v1/projects/{projectRef}/database/query`에 SQL을 전송한다.
- `Inspect`: 두 대상 함수의 `pg_get_functiondef`, `prosecdef`, `provolatile`과 기준 카운트를 읽기 전용 JSON으로 출력한다.
- `Apply`: 적용 전 두 함수 원문을 `-BackupPath`에 저장하고, 파일이 이미 존재하면 덮어쓰지 않고 중단한다. `-SqlPath`의 SQL을 실행한 뒤 즉시 `Verify` 계약을 검사한다.
- 자동 복구: 적용 후 검증이 하나라도 실패하면 같은 프로세스에서 `-BackupPath`의 원문을 재실행하고 exit 1로 종료한다.
- `Verify`: 함수 속성과 `클로드`/`개인` 기준값을 읽기 전용으로 확인하고 JSON을 출력한다.
- `Rollback`: `-BackupPath`의 두 함수 원문만 재실행한 뒤 변경 전 기준값을 확인한다.

스크립트의 고정 검증 SQL은 설정 JSON에서 태그 이름으로 대상 계정과 태그 ID를 찾고, `set_config('request.jwt.claim.sub', user_id, true)`로 로그인 사용자 컨텍스트를 설정한다. 원문 식별은 정확히 다음 signature만 사용한다.

- `get_tag_counts_for_user()`
- `get_books_by_tags(text[],boolean)`

- [x] **Step 3: 운영 함수의 롤백 원문과 기준값이 실제 명령으로 확보된다**

Run: `pwsh -NoProfile -File D:/vibe-coding/my_bookstation/supabase/manage-auto-tag-rpc.ps1 -Action Inspect -ProjectRef ugzruzaywohbynjzjesm -SqlPath D:/vibe-coding/my_bookstation/supabase/20260705_auto_tags_rpc.sql -BackupPath D:/vibe-coding/my_bookstation/supabase/.temp/auto_tag_rpc_preapply.sql`

Expected: exit code 0이며 secret이 없는 JSON이 다음 값을 포함한다.

- `클로드`: 운영 필터 0권, 병합 기대 14권
- `개인`: 운영 필터 355권, 병합 기대 355권
- 2인자 함수: `STABLE + SECURITY INVOKER`, `customTags` 전용
- 백업 파일은 아직 생성되지 않는다.

- [x] **Step 4: 운영 DB 변경 직전 사용자 승인이 명시적으로 확인된다**

Expected: 현재 사용자의 `one-stop` 실행 지시는 `supabase/20260705_auto_tags_rpc.sql`의 두 함수 정의를 운영 프로젝트 `library_book_manager`에 적용하는 승인으로 기록된다.

- [x] **Step 5: 독립 SQL 파일의 두 함수만 운영에 적용된다**

Run: `pwsh -NoProfile -File D:/vibe-coding/my_bookstation/supabase/manage-auto-tag-rpc.ps1 -Action Apply -ProjectRef ugzruzaywohbynjzjesm -SqlPath D:/vibe-coding/my_bookstation/supabase/20260705_auto_tags_rpc.sql -BackupPath D:/vibe-coding/my_bookstation/supabase/.temp/auto_tag_rpc_preapply.sql`

Expected: exit code 0, `applied=true`, `rollback_executed=false`가 출력된다. `get_tag_counts_for_user()`와 `get_books_by_tags(text[], boolean)`가 교체되고, 구형 `get_books_by_tags(text[])`는 변경되지 않는다. `supabase/.temp/project-ref`와 `supabase/migrations/`는 생성되지 않는다.

- [x] **Step 6: 운영 함수 정의와 실데이터가 저장소 계약과 일치한다**

Run: `pwsh -NoProfile -File D:/vibe-coding/my_bookstation/supabase/manage-auto-tag-rpc.ps1 -Action Verify -ProjectRef ugzruzaywohbynjzjesm -SqlPath D:/vibe-coding/my_bookstation/supabase/20260705_auto_tags_rpc.sql -BackupPath D:/vibe-coding/my_bookstation/supabase/.temp/auto_tag_rpc_preapply.sql`

Expected: exit code 0이며 secret이 없는 JSON이 다음 조건을 모두 포함한다.

- 카운트 함수 정의가 `customTags`와 `autoTags`를 모두 포함한다.
- 2인자 필터 함수 정의가 `customTags`와 `autoTags`를 모두 포함한다.
- 2인자 필터 함수는 `STABLE`이다.
- 2인자 필터 함수는 `SECURITY INVOKER`다.
- 2인자 필터 함수의 `filter_by_favorites`에는 기본값이 추가되지 않는다.
- 구형 1인자 함수 정의는 변경 전과 동일하다.
- `클로드`: 카운트 14권, 필터 14권
- `클로드 + 좋아요`: 4권
- `클로드 + AI`: 14권
- `클로드 + AI + 좋아요`: 4권
- `개인`: 카운트 355권, 필터 355권

- [x] **Step 7: 운영 검증 실패 시 기존 함수 정의로 복구할 명령이 고정된다**

Run only when Apply/Verify fails: `pwsh -NoProfile -File D:/vibe-coding/my_bookstation/supabase/manage-auto-tag-rpc.ps1 -Action Rollback -ProjectRef ugzruzaywohbynjzjesm -SqlPath D:/vibe-coding/my_bookstation/supabase/20260705_auto_tags_rpc.sql -BackupPath D:/vibe-coding/my_bookstation/supabase/.temp/auto_tag_rpc_preapply.sql`

Expected: exit code 0, `rolled_back=true`, 변경 전 `클로드` 필터 0권과 `개인` 필터 355권이 출력된다. Apply 내부 자동 복구가 이미 성공했다면 수동 Rollback은 실행하지 않는다.

---

### Task 4: 실제 화면과 저장소 상태가 최종 검증된다

**Files:**
- Read: `components/TagFilter.tsx`
- Read: `stores/useBookStore.ts`
- Modify: `_docs/20260712_01_자동태그-RPC-정합성-수정계획.md`의 실행 증거

**완료 기준:** 새로 연 운영 앱에서 `클로드 (14)`가 표시되고 필터 결과가 14권이며, 저장소에는 계획에서 허용한 SQL·테스트·문서 변경만 존재한다.

- [x] **Step 1: 기존 로그인 Chrome의 새 탭에서 메인 태그 카운트가 갱신된다**

설치된 `chrome:control-chrome` 스킬(`C:/Users/ahnbu/.codex/plugins/cache/openai-bundled/chrome/26.707.31428/skills/control-chrome/SKILL.md`)을 읽고, `mcp__node_repl__js`에서 다음 공식 bootstrap을 실행해 사용자의 기존 Chrome 세션에 연결한다. 스킬이 요구하는 대로 `nodeRepl.write(await chrome.documentation())`의 전체 출력을 먼저 읽은 뒤 그 문서에 공개된 탭 API만 사용한다.

```javascript
if (globalThis.agent?.browsers == null) {
  const { setupBrowserRuntime } = await import(
    "C:/Users/ahnbu/.codex/plugins/cache/openai-bundled/chrome/26.707.31428/scripts/browser-client.mjs"
  );
  await setupBrowserRuntime({ globals: globalThis });
}
if (globalThis.chrome == null) {
  globalThis.chrome = await agent.browsers.get("extension");
  nodeRepl.write(await chrome.documentation());
}
```

`chrome.user.openTabs()`로 탭을 확보하고, 새 탭에서 `https://my-bookstations.vercel.app/`을 연 뒤 로딩이 끝나면 문서화된 `tab.playwright.domSnapshot()`으로 태그 버튼 DOM을 읽는다. 로그인 화면이면 자동으로 자격증명을 입력하지 않고 검증을 중단해 사용자에게 알린다. 사전 파일 확인 `Test-Path C:/Users/ahnbu/.codex/plugins/cache/openai-bundled/chrome/26.707.31428/scripts/browser-client.mjs`는 `True`여야 한다.

Expected: 접근 가능한 button 중 텍스트가 정확히 `클로드 (14)`인 요소가 1개 존재한다.

- [x] **Step 2: 클로드 단일 태그 필터가 전체 서재 기준으로 동작한다**

DOM snapshot에서 확인한 `클로드 (14)` button ref를 `tab.playwright.click()`으로 선택하고, 갱신 후 다시 snapshot을 읽는다.

Expected: 결과 요약에 14권이 표시되고, 화면에 렌더링된 책 카드 수가 14개이며 각 카드의 태그 목록에 `클로드`가 포함된다. DOM 구조상 카드 수를 직접 셀 수 없으면 결과 요약 14권과 Task 3의 운영 RPC 필터 14권을 함께 증거로 기록한다.

- [x] **Step 3: 좋아요·복수 태그 조합이 기존 AND 계약을 유지한다**

Expected:

동일한 Chrome 탭에서 snapshot으로 버튼 ref를 매번 다시 확보한 뒤 `AI` 태그와 좋아요 필터를 순서대로 선택·해제한다. 상태 변경마다 결과 요약 DOM을 다시 읽는다.

- `클로드 + 좋아요`: 결과 요약 4권
- `클로드 + AI`: 결과 요약 14권
- `클로드 + AI + 좋아요`: 결과 요약 4권

화면에 좋아요 필터가 노출되지 않거나 DOM 상호작용이 차단되면, 해당 조합은 Task 3 `Verify`의 운영 RPC 결과를 권위 있는 대체 증거로 사용하고 UI 미검증 사유를 완료 보고에 남긴다.

- [x] **Step 4: 저장소 테스트와 변경 범위가 최종 통과한다**

Run: `node tests/autoTagRpcSql.test.mjs`

Expected: PASS and output contains `auto-tag RPC SQL tests passed`.

Run: `node tests/autoTagRules.test.mjs`

Expected: PASS and output contains `autoTagRules tests passed`.

Run: `git status --short`

Expected: 변경 파일은 다음 구현·검수 산출물뿐이다. `supabase/.temp/auto_tag_rpc_preapply.sql`은 local-only로 남고 커밋 대상에서 제외된다.

- `_docs/20260712_01_자동태그-RPC-정합성-수정계획.md`
- `tests/autoTagRpcSql.test.mjs`
- `supabase/manage-auto-tag-rpc.ps1`
- `supabase/20260705_auto_tags_rpc.sql`
- `README.md`
- `docs/DEVELOPMENT.md`
- `_docs/plan-check/*자동태그-RPC-정합성-수정계획*검수보고서*.md`
- `_docs/done-check/*자동태그-RPC-정합성-수정계획*완료검수보고서.md`

Run: `git diff --check`

Expected: 출력이 없고 exit code 0이다.

## 완료·비완료 보고 형식

- 코드/SQL 소스 수정: 완료 또는 미완료
- 운영 Supabase 함수 적용: 완료 또는 미실행
- 운영 실데이터 검증: 완료 또는 미실행
- 문서 동기화: 완료 또는 미완료
- 커밋: heavy 완료검수 통과 후 `cp`로 실행
- 푸시: `cp`가 remote 존재 시 실행
- Worker 운영배포: 해당 없음

## 자체 검수 결과

- 범위는 운영 RPC 두 개, SQL 계약 테스트, README/DEVELOPMENT 정정으로 제한됐다.
- Supabase 링크와 migration 도입은 명시적으로 제외됐다.
- 구형 1인자 오버로드는 현재 앱 비사용이 확인되어 변경하지 않는다.
- 프론트엔드 로직과 `0권 변경` 문구는 변경하지 않는다.
- 운영 변경에는 사용자 승인과 롤백 원문 확보가 선행된다.
- 완료 기준과 검증 명령의 기대 출력이 모든 Task에 포함됐다.

## 실행 결과

### 계획검수

- agy `Gemini 3.1 Pro (High)` 헤비 검수를 세 차례 수행했다.
- 첫 검수의 운영 DB 명령·롤백 구체성, 두 번째 검수의 UI 검증 구체성 지적을 계획에 반영했다.
- 최종 계획 Gate: `실행 가능`, Critical 없음.
- 검수 원문과 독립 확인 메모는 `_docs/plan-check/`에 보존했다.

### 구현·운영 적용

- `tests/autoTagRpcSql.test.mjs`의 RED가 기존 `default false + SECURITY DEFINER`를 재현했다.
- 2인자 필터 함수는 병합 로직을 유지하면서 필수 boolean 인자, `STABLE + SECURITY INVOKER` 계약을 보존하도록 수정했다.
- `supabase/manage-auto-tag-rpc.ps1`에 `Inspect`, `Apply`, `Verify`, `Rollback`, 적용 전 백업, 검증 실패 시 자동 롤백을 구현했다.
- 운영 적용 전 `deploy-vet` 보안 점검이 통과했다.
- 운영 Apply 결과: `applied=true`, `rollback_executed=false`.
- 로컬 백업 `supabase/.temp/auto_tag_rpc_preapply.sql`은 `.gitignore` 대상이며 커밋하지 않는다.

### 운영 검증 증거

- 전체 서재: 660권
- `클로드`: 수동 0권, 병합 14권, RPC 카운트 14권, 필터 14권
- `클로드 + 좋아요`: 4권
- `클로드 + AI`: 14권
- `클로드 + AI + 좋아요`: 4권
- 대조군 `개인`: 카운트 355권, 필터 355권
- 함수 속성: 2인자 필터 `STABLE`, `SECURITY INVOKER`, 기본 인자 0개
- 구형 `get_books_by_tags(text[])`: 적용 전후 SHA-256 동일
- Chrome 운영 화면: `클로드 (14)`, 단일 필터 `총 14권`, 위 조합 14권·4권을 확인했다.

### 변경 파일

- `supabase/20260705_auto_tags_rpc.sql`
- `supabase/manage-auto-tag-rpc.ps1`
- `tests/autoTagRpcSql.test.mjs`
- `README.md`
- `docs/DEVELOPMENT.md`
- 본 계획서와 `_docs/plan-check/`, `_docs/done-check/` 검수 보고서

### 제한·비변경 사항

- 구형 1인자 RPC, 프론트엔드, 자동태그 재계산 문구는 변경하지 않았다.
- Supabase project link와 migrations 체계는 도입하지 않았다.
- Worker 변경과 운영배포는 없다.

### 완료검수

- agy `Gemini 3.1 Pro (High)` 헤비 완료검수 Gate: `Pass`.
- Critical 없음. 기존 Vite 500 kB chunk-size 경고만 advisory로 분류했다.
- 완료검수 보고서: `_docs/done-check/20260712_01_자동태그-RPC-정합성-수정계획_완료검수보고서.md`.
